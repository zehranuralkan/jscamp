export default class User {
    constructor(id, firstName, lastName, city) {
        //oluşturucu. Bu bir class'dır
        this.id = id
        this.firstName = firstName
        this.lastName = lastName
        this.city = city
    }
}